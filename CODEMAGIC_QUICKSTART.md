# Codemagic - Быстрый старт

## 🚀 Быстрая настройка (5 минут)

### 1. Подготовка проекта

```bash
# Установите Capacitor (если еще не установлен)
pnpm add -D @capacitor/cli @capacitor/core @capacitor/ios

# Инициализируйте Capacitor
pnpm cap:init
# Введите: App name: "Composio Chat"
# Введите: Package ID: "com.yourcompany.composio-chat"

# Добавьте iOS платформу
pnpm cap:add:ios

# Синхронизируйте
pnpm cap:sync
```

### 2. Настройка Codemagic

1. **Зарегистрируйтесь на [codemagic.io](https://codemagic.io)**
2. **Подключите ваш репозиторий** (GitHub/GitLab/Bitbucket)
3. **Создайте App Store Connect API ключ:**
   - Перейдите в [App Store Connect](https://appstoreconnect.apple.com)
   - **Users and Access** → **Keys** → **App Store Connect API**
   - Создайте ключ с ролью **App Manager**
   - Скачайте `.p8` файл (доступен только один раз!)
   - Запишите **Key ID** и **Issuer ID**

4. **Добавьте credentials в Codemagic:**
   - Settings → **App Store Connect** → **Add credentials**
   - Выберите **App Store Connect API key**
   - Загрузите `.p8` файл
   - Введите **Key ID** и **Issuer ID**

5. **Создайте группу переменных:**
   - Settings → **Environment variables** → **Groups**
   - Создайте группу `app_store_credentials`
   - Добавьте переменные (если используете API key, они добавятся автоматически)

6. **Обновите codemagic.yaml:**
   ```yaml
   vars:
     APP_ID: "com.yourcompany.composio-chat"  # ← Ваш Bundle ID
   ```

7. **Обновите export_options.plist:**
   - Замените `YOUR_TEAM_ID` на ваш Team ID из Apple Developer

### 3. Запуск первой сборки

1. В Codemagic выберите ваш проект
2. Нажмите **Start new build**
3. Выберите ветку (обычно `main`)
4. Выберите workflow `ios-workflow`
5. Нажмите **Start build**

### 4. После успешной сборки

- Скачайте `.ipa` файл из артефактов
- Или настройте автоматическую публикацию в TestFlight (раскомментируйте в `codemagic.yaml`)

## ⚠️ Важные замечания

1. **Bundle ID должен быть уникальным** - используйте формат `com.yourcompany.appname`
2. **Team ID** можно найти в [Apple Developer](https://developer.apple.com/account) → **Membership**
3. **Первый запуск может занять 15-20 минут** - Codemagic устанавливает зависимости и настраивает окружение
4. **Миграции БД пропускаются** - если нужны миграции, добавьте переменные окружения для БД

## 📚 Подробная документация

См. [CODEMAGIC_SETUP.md](./CODEMAGIC_SETUP.md) для детальной инструкции.

## 🆘 Проблемы?

- **Ошибка сборки?** Проверьте логи в Codemagic
- **Проблемы с подписью?** Убедитесь, что App Store Connect API key настроен правильно
- **Capacitor не найден?** Убедитесь, что выполнили `pnpm cap:add:ios` локально
