# Настройка PWA и iOS Web View

Приложение настроено для работы как PWA (Progressive Web App) и iOS Web View.

## ✅ Что уже настроено:

1. **next-pwa** - установлен и настроен
2. **manifest.json** - создан в `public/manifest.json`
3. **Мета-теги для iOS** - добавлены в `app/layout.tsx`
4. **Конфигурация PWA** - настроена в `next.config.ts`
5. **Service Worker** - будет автоматически создан при сборке

## 📱 Что нужно сделать:

### 1. Создать иконки для PWA

Поместите следующие файлы в папку `public/`:

- `icon-192x192.png` (192x192 пикселей)
- `icon-512x512.png` (512x512 пикселей)
- `apple-touch-icon.png` (180x180 пикселей)

**Способы создания иконок:**
- Используйте онлайн генераторы: [PWA Asset Generator](https://github.com/onderceylan/pwa-asset-generator)
- Или создайте вручную из вашего логотипа

### 2. Обновить URL в manifest.json

Откройте `public/manifest.json` и обновите `metadataBase` в `app/layout.tsx` на ваш домен:

```typescript
// app/layout.tsx
export const metadata: Metadata = {
  metadataBase: new URL('https://ваш-домен.com'), // ← измените здесь
  // ...
};
```

### 3. Тестирование PWA

1. **В браузере:**
   - Откройте приложение в Chrome/Edge
   - Откройте DevTools → Application → Service Workers
   - Проверьте, что Service Worker зарегистрирован
   - Проверьте Manifest

2. **На iOS:**
   - Откройте сайт в Safari
   - Нажмите кнопку "Поделиться" → "На экран «Домой»"
   - Приложение появится на главном экране как нативное

3. **На Android:**
   - Chrome предложит установить приложение автоматически
   - Или используйте меню → "Добавить на главный экран"

## 🔧 Особенности конфигурации:

- **PWA отключен в development** - работает только в production
- **Кэширование настроено** - статические ресурсы кэшируются автоматически
- **iOS Web View** - поддерживается через мета-теги
- **Standalone режим** - приложение открывается без адресной строки

## 📝 Публикация в App Store (опционально):

### 🎯 Рекомендуемый способ: PWA Builder (самый простой!)

**PWA Builder** автоматически создает iOS проект из вашего PWA - без Capacitor, без сложной настройки!

**Быстрый старт:**
1. Задеплойте ваше PWA приложение (Vercel, Netlify и т.д.)
2. Откройте [PWA Builder](https://www.pwabuilder.com/)
3. Введите URL вашего приложения
4. Скачайте готовый iOS проект
5. Откройте в Xcode и опубликуйте

**Подробная инструкция:** См. [PWA_BUILDER_GUIDE.md](./PWA_BUILDER_GUIDE.md)

### Альтернативные способы:

1. **Capacitor + Codemagic** - для автоматической сборки и публикации
   - Подробнее: [CODEMAGIC_SETUP.md](./CODEMAGIC_SETUP.md)
   - Требует больше настройки, но дает полный контроль

2. **Только PWA (без App Store)** - пользователи устанавливают напрямую
   - iOS: Safari → Поделиться → На экран «Домой»
   - Android: Chrome предложит установку автоматически
   - Самый простой вариант - никакой настройки не требуется!

## 🚀 Деплой:

После создания иконок и обновления URL, просто задеплойте приложение:

```bash
pnpm build
# Затем деплой на Vercel или другую платформу
```

PWA будет автоматически активирован в production режиме!
